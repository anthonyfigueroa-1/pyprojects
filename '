def main():
    balance = int(1000)
    change = int(0)
    keepgoing = True

    print ("Welcome to the ATM!")

    while keepgoing:
        print ("1. Check balance")
        print ("2. Deposit")
        print ("3. Withdraw")
        print ("4. Exit")
        option = input (int("Choose an option: "))
        
        match option:
            case "1":
                print (f"Current balance is ${balance}")
            case "2":
                change = intput, int("How much would you like to deposit: ")
                balance += change
                print (f"Deposited ${change}. New balance is ${balance}")
            case "3":
                withdraw = int
                withdraw = input, int("How much would you like to withdraw: ")
                balance -= withdraw
            case "4":
                print("Thank you, come again!")
                keepgoing = False

if __name__ == "__main__":
    main()
